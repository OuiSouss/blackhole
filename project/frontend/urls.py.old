from django.contrib import admin
from django.urls import include, path

#écrire n'importe quoi = pas de conséquences
urlpatterns = [
    path('admin/', admin.site.urls),
    path('accounts/', include('django.contrib.auth.urls')),
]
